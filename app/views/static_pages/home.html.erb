<% if signed_in? %>
    <div class="row">
        <aside class="span4">
            <section>
                <%= render 'shared/user_info' %>
            </section>
            <section>
                <%= render 'shared/comment_form' %>
            </section>
        </aside>
    </div>
<% else %>
    Home
<% end %>
